// No middleware needed for localStorage-based language switching
// Language is handled client-side via Providers component

export const config = {
  // Don't match any routes - we're not using URL-based routing
  matcher: []
};
